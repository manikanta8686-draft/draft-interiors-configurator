import {useMemo,useState} from "react";import {Link} from "react-router-dom";import {motion} from "framer-motion";import {ArrowUpRight} from "../components/Icons";import {categories,sofaModels} from "../data/sofas";
export default function Collection(){const [filter,setFilter]=useState("All"),visible=useMemo(()=>filter==="All"?sofaModels:sofaModels.filter(s=>s.category===filter),[filter]);return <main className="collection-page"><div className="page-intro"><p className="eyebrow">THE SOFA COLLECTION</p><h1>Made for the way<br/><em>you live.</em></h1><p>Twenty considered forms, exceptional comfort, and materials chosen to grow more beautiful over time.</p></div><div className="collection-filters">{categories.map(x=><button className={filter===x?"selected":""} key={x} onClick={()=>setFilter(x)}>{x}</button>)}</div><section className="collection-grid">{visible.map((s,i)=><motion.article className="sofa-product" key={s.id} initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} transition={{delay:i*.04}}><img src={s.image} alt={s.name}/><p className="eyebrow">{s.category}</p><h2>{s.name}</h2><p>{s.description}</p><dl><div><dt>Starting at</dt><dd>{`\u20B9${s.price.toLocaleString("en-IN")}`}</dd></div><div><dt>Dimensions</dt><dd>{s.dimensions}</dd></div></dl><p className="fabric-note">{s.fabrics.join(" · ")}</p><Link className="configure-link" to={`/configurator?model=${s.id}`}>Configure <ArrowUpRight/></Link></motion.article>)}</section></main>}
